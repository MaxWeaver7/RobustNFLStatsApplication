# Example environment file.
# Usage:
#   cp config/env.example .env   (if your environment allows dotfiles)
# Or export vars directly in your shell.

# ----------------------------
# Supabase + BALLDONTLIE (new)
# ----------------------------
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
BALLDONTLIE_API_KEY=

# Optional ingestion flags
# - Set to 1 to attempt GOAT advanced endpoints (may be unstable at times)
BDL_INCLUDE_ADVANCED=0
# - Set to 1 to run ONLY advanced endpoints (skips core + season/game stats)
BDL_ADVANCED_ONLY=0

# SQLite DB path (absolute or relative to project root)
NFL_DB_PATH=data/nfl_data.db

# Comma-separated seasons to ingest from nflfastR via nfl_data_py
NFL_SEASONS=2024,2025

# Best-effort PFR scraping controls (optional)
PFR_ENABLE=false
PFR_REQUEST_DELAY_SECONDS=2.5
PFR_CACHE_DIR=data/pfr_cache


